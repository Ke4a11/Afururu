<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/background_material_light</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--半透明背景設定 android:theme="@style/Theme.TranslucentBackground"-->
    <drawable name="transparameter">#bb000000</drawable>
    <style name="Theme.TranslucentBackground" parent="android:style/Theme.Translucent">
        <item name="android:windowBackground">@drawable/transparameter</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!--android:theme="@style/Theme.MyTrans"-->
    <drawable name="translucent_background">#44FF0000</drawable>
    <style name="Theme.MyTrans" parent="android:style/Theme.Translucent">
        <item name="android:windowBackground">@drawable/translucent_background</item>
    </style>

    <!-- アニメーションの詳細設定 res>animatorに詳しい内容 -->
    <style name="Animation" parent="android:Animation" />
    <style name="Animation.Activity" parent="android:Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@animator/slide_in_under</item>
        <item name="android:activityOpenExitAnimation">@animator/slide_out_under</item>
    </style>

    <color name="my_spinner_text_color">#353535</color>

</resources>
